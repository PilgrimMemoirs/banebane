<% @tasks.each do |task| %>
  <%=link_to "#{task.id}", task%>
  <%= link_to "Delete", task, :method => :delete %>
  <% if task.completed_at %>
    COMPLETED
  <%else%>
    <%= link_to "Mark as Complete", task, :method => :put%>
  <%end%>
  <br>
<%end%>

<%=link_to "New Task", "tasks/new"%>